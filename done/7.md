# 7 - Epic: MoonPay coding challenge

# Description

This is a coding challenge, the goal of which is to productionalize the deployment of a Next.js application.

See `./README.md` and `./DEVELOPMENT.md` for an overview.

There are two major requirements we must break into multiple tasks.

Requirement 1: Containerize the Application
  - Write a `Dockerfile` to containerize the application.
  - Ensure it follows best practices for a Next.js application.
  - Build and run the container locally to verify it works.

Requirement 2: Deploy the Application
- Deploy the application to **Kubernetes** (local k3s cluster)
  - Helm
  - Github Actions
  - Postgres DB will also have to run in Kubernetes
    - This also needs a Helm chart
    - We should run Postgres as a Statefulset with PVC for storage
- Ensure the solution is **as close to production-ready as possible**. Consider aspects like:
  - Security (run as non-root, minimal base image, no priveleged containers, shift-left package scanning, etc)
  - Scalability (autoscaling, etc.)
  - Reliability (deployments, probes, etc.)
    - note for probes: it looks like we will have to create health checks (a simple one and a database-connection-checking one)
- Demonstrate that the application is reachable and returns the _Latest Crypto Prices_.
  - e.g.: post deploy smoke tests

Other requirements:
- Use Terraform for any other infrastructure required by the application, if relevant.
- All Terraform, Helm, and any other new artifacts should live in this repo (as opposed to their own repo).

Notes about the Kubernetes cluster setup:
- The cluster is k3s running on another laptop in my lan
- The cluster runs on a private network (libvirt NAT), so we expose services via an SSH tunnel, similar to a bastion host pattern in production environments
- For simplicity, we can use `/etc/hosts` as local "DNS"
- The cluster runs open source Traefik
- K3s comes with Rancher's Local Path Provisioner (`storageClassName: local-path`) 

# PRD

## Introduction / Overview

This epic covers the end-to-end productionalization of the MoonPay DevOps Challenge application — a Next.js 16 / TypeScript app backed by PostgreSQL 17, displaying live cryptocurrency prices. The work involves containerizing the application, deploying it to a local k3s Kubernetes cluster via Helm and GitHub Actions CI/CD, and ensuring the result is as close to production-grade as possible. This will be presented to MoonPay interviewers on Tuesday, 2026-04-21.

## Goals

1. Produce a production-quality `Dockerfile` using multi-stage builds that minimizes the final image size.
2. Publish images to GitHub Container Registry (GHCR) via GitHub Actions on every push to `main`.
3. Deploy the Next.js app and a PostgreSQL 17 StatefulSet to a local k3s cluster using separate Helm charts.
4. Meet production-readiness standards: security, scalability (HPA), and reliability (probes, rolling deployments).
5. Verify the deployment end-to-end with a post-deploy smoke test.
6. Document and automate the SSH tunnel setup used both for local access and for GitHub Actions deployments.

## User Stories

- As a **developer**, I can build and run the containerized app locally with a single `docker build` + `docker run` so I can iterate quickly.
- As a **CI/CD pipeline**, I can build, scan, push, and deploy the app automatically on every merge to `main` so no manual steps are needed for deployment.
- As a **cluster operator**, I can install or upgrade the app and its database with `helm upgrade --install` so deployments are repeatable and version-controlled.
- As an **interviewer**, I can hit the app through the SSH tunnel and see the _Latest Crypto Prices_ page so I can verify the solution works end-to-end.
- As an **on-call engineer**, I can query `/api/health/live` and `/api/health/ready` to quickly determine whether the app and its database connection are healthy.

## Functional Requirements

### Requirement 1 — Containerize the Application

1. Write a multi-stage `Dockerfile` using `node:22-alpine` as both the build and runtime base image.
   - **Stage 1 (deps):** Install only production `node_modules` using `pnpm`.
   - **Stage 2 (builder):** Copy source, install all deps (including dev), run `pnpm build`.
   - **Stage 3 (runner):** Copy built output and production `node_modules` only; run as a non-root user (`nextjs`/UID 1001).
2. Set `output: 'standalone'` in `next.config.ts` to minimize the runtime image size (recommended approach for Docker/Kubernetes deployments).
3. The container must accept `POSTGRES_PRISMA_URL` as a runtime environment variable (not baked into the image).
4. The container must expose port `3000`.
5. The image must pass a Trivy vulnerability scan with no CRITICAL or HIGH CVEs in the final stage (shift-left scanning in CI).
6. The existing `docker-compose.yaml` `nextjs` service must continue to work with the new `Dockerfile`.

### Requirement 2 — Health Check API Routes

7. Add a `GET /api/health/live` route to the Next.js app that returns `200 OK` with `{"status":"ok"}`. Used as a Kubernetes **liveness** probe.
8. Add a `GET /api/health/ready` route that attempts a lightweight Prisma DB query (e.g., `SELECT 1`) and returns `200 OK` if successful, or `503 Service Unavailable` if the DB is unreachable. Used as a Kubernetes **readiness** probe.

### Requirement 3 — Postgres Helm Chart

9. Use the Bitnami `postgresql` Helm chart to deploy PostgreSQL 17.
10. Configure a `StatefulSet` with a `PersistentVolumeClaim` using `storageClassName: local-path` (k3s Local Path Provisioner).
11. Database credentials (username, password, database name) must be stored in a Kubernetes `Secret` and referenced by the chart values — not hardcoded in values files.
12. The chart values file must live in this repo at `helm/postgres/values.yaml`.

### Requirement 4 — App Helm Chart

13. Write a Helm chart for the Next.js app at `helm/app/`.
14. The chart must include:
    - `Deployment` with rolling update strategy.
    - `Service` (ClusterIP).
    - `Ingress` configured for Traefik (the k3s default ingress controller), using hostname `devops-challenge.local`.
    - `HorizontalPodAutoscaler` with `minReplicas: 2`, `maxReplicas: 3`, targeting CPU utilization.
    - `Secret` for `POSTGRES_PRISMA_URL` injected as an env var into the pod using Kubernetes Secrets.
15. Containers must run as non-root (`runAsNonRoot: true`, `runAsUser: 1001`).
16. No privileged containers; drop `ALL` Linux capabilities.
17. Liveness probe: `GET /api/health/live`, initial delay 15s, period 10s.
18. Readiness probe: `GET /api/health/ready`, initial delay 10s, period 5s.
19. Resource requests and limits must be defined (reasonable defaults for a 3-node k3s cluster whose worker nodes have 1 cpu and 3145728 KiB memory).
20. Prisma migrations must run as a Kubernetes `Job` annotated as a Helm `pre-upgrade` (and `pre-install`) hook, executing `pnpm db:migrate` before the app `Deployment` is updated. The Job must use the same `POSTGRES_PRISMA_URL` secret as the app. Set `hook-delete-policy: before-hook-creation` so old Job pods are cleaned up on each deploy.

### Requirement 5 — GitHub Actions CI/CD

21. The workflow must run on a **self-hosted GitHub Actions runner** registered on the same LAN as the k3s cluster, giving it direct network access without any SSH tunnel. The runner should be documented in `docs/self-hosted-runner.md`.
22. On push to `main`, a GitHub Actions workflow at `.github/workflows/deploy.yml` must:
    1. Build the Docker image.
    2. Scan the image with Trivy; fail the pipeline on CRITICAL/HIGH CVEs.
    3. Push the image to GHCR using `secrets.GHCR_TOKEN`. Tag with the Git SHA and `latest`.
    4. Deploy via `helm upgrade --install` using a kubeconfig stored in `secrets.KUBECONFIG` (the Prisma migration Job runs automatically as a pre-upgrade hook).
    5. Run the smoke test script as the final step.
23. Store the following as repository secrets:
    - `KUBECONFIG`: the k3s kubeconfig pointing directly to the cluster API server (no tunnel patching required).
    - `GHCR_TOKEN`: a GitHub PAT with `write:packages` scope.
24. Image naming convention: `ghcr.io/<github-org>/devops-challenge:<sha>` and `ghcr.io/<github-org>/devops-challenge:latest`.

### Requirement 6 — SSH Tunnel / Local Developer Access

25. The k3s cluster runs on a private LAN (libvirt NAT) and is not directly reachable from the developer's laptop. Local access (for development and demo purposes) must go through an SSH tunnel to the k3s host machine.
26. Document the SSH tunnel setup in `docs/ssh-tunnel.md`, covering:
    - How the tunnel works (SSH port-forward from developer laptop → k3s host → cluster).
    - Command to open the tunnel, forwarding the Traefik ingress port (`80`) and the k3s API server port (`6443`).
    - The `/etc/hosts` entry needed to resolve `devops-challenge.local` locally.
    - How to export the kubeconfig and patch the server URL to `localhost` for local use over the tunnel.
    - How to verify connectivity before running a demo or local deploy.
27. Provide a shell script `scripts/tunnel.sh` that opens the SSH tunnel in the foreground (ingress + API server ports).

### Requirement 7 — Post-Deploy Smoke Test

28. A smoke test script `scripts/smoke-test.sh` must:
    - `curl` `http://devops-challenge.local` and assert HTTP 200.
    - Check that the response body contains an expected string (e.g., `"Latest Crypto Prices"` or a known currency code like `"BTC"`).
    - Exit non-zero on failure with a descriptive error message.
29. The smoke test must be the final step in the GitHub Actions deploy workflow (run after `helm upgrade --install` completes).

### Requirement 8 — Terraform

30. Terraform is not required for this setup. Add a note to the repo README explicitly stating that Terraform was evaluated and determined to be out of scope for a local k3s environment.

## Non-Goals

- No GKE or cloud Kubernetes — local k3s only.
- No Terraform (unless a concrete need emerges).
- No TLS termination beyond Traefik defaults (no cert-manager).
- No external DNS — `/etc/hosts` is sufficient.
- No Playwright or browser-based smoke testing — `curl` is sufficient.
- No multi-environment (staging/prod) setup — single environment only.

## Design Considerations

- The Postgres connection string should be assembled from individual secret values (host, port, user, password, db name) rather than storing the full URL as a single opaque secret, to make credential rotation easier.
- Use `imagePullPolicy: Always` in the app Deployment so the `latest` tag always pulls the freshest image after each CI deploy.
- The self-hosted runner eliminates the need for any SSH tunnel in CI. The `scripts/tunnel.sh` and `docs/ssh-tunnel.md` are for local developer access and demo use only.

## Technical Considerations

- **Node.js:** 22 LTS (`node:22-alpine`)
- **Postgres:** 17 (Bitnami chart, image tag pinned to `17`)
- **pnpm:** The `Dockerfile` must install `corepack` and enable pnpm before installing dependencies.
- **Next.js standalone output:** `output: 'standalone'` in `next.config.ts` is required for the multi-stage Docker build to produce a minimal runtime image.
- **Prisma migrations:** The migration `Job` runs as a Helm `pre-install`/`pre-upgrade` hook with `hook-delete-policy: before-hook-creation`. Helm will wait for the Job to complete before proceeding with the Deployment rollout, so no explicit `kubectl` step is needed in CI.
- **kubeconfig for CI:** The kubeconfig stored in `secrets.KUBECONFIG` points directly to the k3s API server's LAN IP/port. No URL patching is needed since the self-hosted runner has direct network access.

## Success Metrics

1. `docker build` completes without errors and the final image is under 300 MB.
2. Trivy reports zero CRITICAL/HIGH CVEs in the final image layer.
3. `helm upgrade --install` completes without errors on the k3s cluster.
4. All pods reach `Running` state; readiness probe passes within 60 seconds of deployment.
5. HPA is present and reports correct `minReplicas: 1` / `maxReplicas: 3` configuration.
6. Smoke test exits 0 — HTTP 200 returned and body contains expected content.
7. Full GitHub Actions pipeline (build → scan → push → migrate → deploy → smoke test) completes green on a push to `main` using the self-hosted runner.

## Open Questions

None.

# PLAN

## Relevant Files

- `Dockerfile` – new multi-stage production image (deps → builder → runner)
- `.dockerignore` – update to exclude `docs/`, `helm/`, `scripts/`, `prisma/migrations` source files not needed in image
- `next.config.ts` – add `output: 'standalone'` to enable minimal runtime image
- `app/api/health/live/route.ts` – new liveness probe endpoint
- `app/api/health/ready/route.ts` – new readiness probe endpoint (DB connectivity check)
- `lib/prisma.ts` – referenced by the ready route; Prisma client uses `prisma/generated/`
- `package.json` – add `db:migrate:deploy` script using `prisma migrate deploy`
- `helm/postgres/values.yaml` – Bitnami postgresql chart values
- `helm/app/Chart.yaml` – app chart metadata
- `helm/app/values.yaml` – default values (image, resources, ingress host, HPA, etc.)
- `helm/app/templates/deployment.yaml` – Deployment with probes, security context, resource limits
- `helm/app/templates/service.yaml` – ClusterIP Service
- `helm/app/templates/ingress.yaml` – Traefik Ingress for `devops-challenge.local`
- `helm/app/templates/hpa.yaml` – HorizontalPodAutoscaler (min 2, max 3)
- `helm/app/templates/secret.yaml` – Secret for `POSTGRES_PRISMA_URL`
- `helm/app/templates/migrate-job.yaml` – pre-install/pre-upgrade hook Job for migrations
- `.github/workflows/deploy.yml` – CI/CD pipeline (build → scan → push → deploy → smoke test)
- `scripts/tunnel.sh` – opens SSH tunnel for local developer access
- `scripts/smoke-test.sh` – post-deploy curl-based smoke test
- `docs/ssh-tunnel.md` – local access setup guide
- `docs/self-hosted-runner.md` – self-hosted runner setup guide
- `README.md` – update with deployment overview and Terraform out-of-scope note

## Notes

- `pnpm db:migrate` runs `prisma migrate dev` which is interactive and dev-only. The migration Job must use `prisma migrate deploy` instead. Add a `db:migrate:deploy` script to `package.json` (e.g. `prisma migrate deploy`) and use that in the Job.
- The Prisma generated client lives at `prisma/generated/` and is not auto-included in the Next.js standalone output. The runner stage of the Dockerfile must explicitly copy it: `COPY --from=builder /app/prisma/generated ./prisma/generated`.
- k3s requires `metrics-server` to be installed for HPA to function. The self-hosted runner docs should note this as a cluster prerequisite.
- The self-hosted runner machine needs the cluster's LAN IP (not `127.0.0.1`) mapped to `devops-challenge.local` in its `/etc/hosts` since it has direct network access and does not use the tunnel.
- `scripts/tunnel.sh` forwards local **8080 → remote 80** for Traefik (port 80 requires root and cannot be bound directly). Local curl and smoke test use `http://devops-challenge.local:8080`.
- `scripts/tunnel.sh` checks whether port 6443 is already in use before opening that forward, since it is frequently kept open.
- HPA `minReplicas` is `2` per updated PRD requirement (user edited this from 1 → 2).
- For local Helm installs, the `POSTGRES_PRISMA_URL` secret must be created manually before running `helm install`. Document the `kubectl create secret` command in `docs/ssh-tunnel.md` or a dedicated ops runbook.

## Subtasks

- [x] 1.0 Containerize the application
  - [x] 1.1 Add `output: 'standalone'` to `next.config.ts`
  - [x] 1.2 Write the three-stage `Dockerfile`: stage 1 installs production deps with pnpm, stage 2 installs all deps and runs `pnpm build`, stage 3 copies `.next/standalone`, `.next/static`, `public/`, and `prisma/generated/` — runs as user `nextjs` (UID 1001)
  - [x] 1.3 Update `.dockerignore` to also exclude `helm/`, `scripts/`, `docs/`, `.github/`, and `prisma/migrations`
  - [x] 1.4 Verify `docker compose up` still works end-to-end (app connects to Postgres and returns crypto prices)
  - [x] 1.5 Run a local Trivy scan (`trivy image`) and resolve any CRITICAL/HIGH CVEs before moving to CI

- [x] 2.0 Add health check API routes
  - [x] 2.1 Create `app/api/health/live/route.ts` — export a `GET` handler that returns `Response.json({ status: 'ok' })` with status 200
  - [x] 2.2 Create `app/api/health/ready/route.ts` — import the Prisma client from `lib/prisma.ts`, run `prisma.$queryRaw\`SELECT 1\``, return 200 on success or 503 with `{ status: 'unavailable', error: string }` on failure
  - [x] 2.3 Test both routes locally with `curl http://localhost:3000/api/health/live` and `curl http://localhost:3000/api/health/ready`

- [x] 3.0 Write the Postgres Helm chart
  - [x] 3.1 Create `helm/postgres/` directory and add the Bitnami repo locally (`helm repo add bitnami https://charts.bitnami.com/bitnami`)
  - [x] 3.2 Write `helm/postgres/values.yaml` pinning the image to PostgreSQL 17, setting `storageClass: local-path`, enabling persistence, and referencing an existing Secret for credentials (using `global.postgresql.auth.existingSecret`)
  - [x] 3.3 Document the one-time `kubectl create secret` command needed to create the credentials Secret before the first `helm install`
  - [x] 3.4 Do a dry-run install (`helm install --dry-run`) against the cluster to validate the values file

- [x] 4.0 Write the app Helm chart
  - [x] 4.1 Scaffold `helm/app/` with `Chart.yaml` and a `values.yaml` covering: image repo/tag, ingress host (`devops-challenge.local`), resource requests/limits, HPA min/max, and replica count
  - [x] 4.2 Write `helm/app/templates/secret.yaml` — creates a Kubernetes Secret holding `POSTGRES_PRISMA_URL`, with the value templated from `values.yaml` (never hardcoded)
  - [x] 4.3 Write `helm/app/templates/deployment.yaml` — rolling update strategy; `securityContext: { runAsNonRoot: true, runAsUser: 1001 }`; `capabilities: { drop: [ALL] }`; `imagePullPolicy: Always`; env var from the Secret; liveness probe (`/api/health/live`, initialDelaySeconds: 15, periodSeconds: 10); readiness probe (`/api/health/ready`, initialDelaySeconds: 10, periodSeconds: 5); resource requests (`cpu: 100m, memory: 256Mi`) and limits (`cpu: 500m, memory: 512Mi`)
  - [x] 4.4 Write `helm/app/templates/service.yaml` — ClusterIP Service on port 80 targeting container port 3000
  - [x] 4.5 Write `helm/app/templates/ingress.yaml` — Traefik IngressRoute or standard `networking.k8s.io/v1` Ingress with host `devops-challenge.local` and annotation `kubernetes.io/ingress.class: traefik`
  - [x] 4.6 Write `helm/app/templates/hpa.yaml` — HPA targeting the Deployment, `minReplicas: 2`, `maxReplicas: 3`, CPU utilization target 70%
  - [x] 4.7 Write `helm/app/templates/migrate-job.yaml` — Kubernetes Job with annotations `helm.sh/hook: pre-install,pre-upgrade` and `helm.sh/hook-delete-policy: before-hook-creation`; runs the app image with command `pnpm db:migrate:deploy`; uses the same `POSTGRES_PRISMA_URL` Secret
  - [x] 4.8 Add `db:migrate:deploy` script to `package.json` that runs `prisma migrate deploy` (non-interactive, safe for production/CI use)
  - [x] 4.9 Do a `helm template` dry-run and a `helm install --dry-run` to validate all templates render correctly

- [x] 5.0 Set up GitHub Actions CI/CD
  - [x] 5.1 Create `.github/workflows/deploy.yml` with `on: push: branches: [main]` and `runs-on: self-hosted`
  - [x] 5.2 Add build step: `docker build -t ghcr.io/${{ github.repository }}:${{ github.sha }} .`
  - [x] 5.3 Add Trivy scan step using `aquasecurity/trivy-action`; set `exit-code: 1`, `severity: CRITICAL,HIGH`
  - [x] 5.4 Add GHCR login and push step using `secrets.GHCR_TOKEN`; push both the SHA tag and `latest`
  - [x] 5.5 Add Helm deploy step: write `secrets.KUBECONFIG` to a temp file, then run `helm upgrade --install` for the app chart (Postgres chart is installed once manually and not managed by CI)
  - [x] 5.6 Add smoke test step that calls `scripts/smoke-test.sh`
  - [x] 5.7 Write `docs/self-hosted-runner.md` covering: prerequisites (Docker, Helm, kubectl, pnpm), how to register the runner (`gh` CLI or GitHub UI), required `/etc/hosts` entry for `devops-challenge.local`, and required GitHub repository secrets (`KUBECONFIG`, `GHCR_TOKEN`)

- [x] 6.0 Cluster access tooling, smoke test, and docs
  - [x] 6.1 Write `scripts/tunnel.sh` — before opening each forward, check whether the port is already in use (e.g. `lsof -ti:<port>`) and skip it if so; forward port 6443 (k3s API, often already open) and forward local port **8080** → remote port **80** (Traefik ingress; port 80 requires root so 8080 is used locally); make it executable (`chmod +x`)
  - [x] 6.2 Write `scripts/smoke-test.sh` — accept an optional `BASE_URL` env var defaulting to `http://devops-challenge.local:8080` for local tunnel use (CI can override to `http://devops-challenge.local` if it hits Traefik on 80 directly); `curl -sf $BASE_URL` and grep for `"Latest Crypto Prices"` or `"BTC"`; print a clear pass/fail message; exit 1 on failure; make it executable
  - [x] 6.3 Write `docs/ssh-tunnel.md` covering: network topology diagram (laptop → SSH → k3s host → cluster), the tunnel command, required `/etc/hosts` entry (`127.0.0.1 devops-challenge.local`), note that local port **8080** maps to cluster port 80 (and that 6443 is skipped if already forwarded), how to patch the kubeconfig server URL to `localhost:6443` for local kubectl use, and connectivity verification steps
  - [x] 6.4 Update `README.md` to add a deployment section summarising the Helm charts, CI/CD pipeline, and self-hosted runner; include a note that Terraform was evaluated and is out of scope for this local k3s setup

# PRs

| Branch | PR |
|--------|----|
| `feature/7` | https://github.com/creisor/devops-challenge/pull/1 |

Branch is up to date with `main`. All plan tasks verified. Ready to merge.
